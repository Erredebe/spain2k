[build]
  command = "export VITE_BUILD_SHA=$COMMIT_REF && export VITE_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) && npm ci && npm run lint && npm test && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-fund --no-audit"
